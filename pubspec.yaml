name: fluttelog
description: A new flutter plugin project.
version: 0.0.1
homepage: https://github.com/Coalery/FlutteLog

environment:
  sdk: ">=2.7.0 <3.0.0"
  flutter: ">=1.20.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_markdown: ^0.6.2

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  plugin:
    platforms:
      web:
        pluginClass: Fluttelog
        fileName: fluttelog.dart
